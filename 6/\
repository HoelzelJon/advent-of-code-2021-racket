(define inputs (map string->number (string-split (car (file->lines "input.txt")) ",")))

(define (fish-countdown n)
  (if (= n 0) 6 (- n 1)))

(println "test")

(define (fish-step fish)
  (append
    ()
    ()))
